import React from "react";

const ProductForm = ({
  product,
  productImage,
  imagePreview,
  description,
  setDescripion,
  handleInputChange,
  HandleImageChange,
  SaveProduct,
}) => {
  return <div></div>;
};

export default ProductForm;
